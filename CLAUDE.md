# Claude / AI メモ

このファイルはエージェント向けの文脈共有用。実装の詳細は README・`docs/requirements.md` を参照。

## Design Context

### Users

- **主な利用者**: 転職エージェント、応募先企業の採用・面接担当者（要件: `docs/requirements.md`）。
- **文脈**: URL を共有された採用担当が **`/portfolio` をそのまま**（ブラウザの Basic ダイアログなしで）短時間に読む。一般向けの広告・検索流入は想定せず、**検索・インデックス抑制**（`noindex`、`robots.txt`、**`X-Robots-Tag`**）が前提。
- **達成してほしいこと（ジョブ）**: 実績と思考プロセスを把握し、「この人と話したい」と判断する材料を、無駄なノイズなく得ること。

### Brand Personality

- **トーン**: 落ち着きと信頼。静かで誠実に、読み物として成立するインターフェース（本人確認済みの志向）。
- **言語**: 日本語を主軸にしたコピー。英語ラベル（例: section 見出し）は意図があれば可だが、一貫した声を優先する。
- **3 語で**: *Calm（落ち着き）* · *Trustworthy（信頼）* · *Text-forward（テキスト主導）*
- **感情目標**: 不安を煽らず、品質と判断の筋が伝わる「安心して読める」体験。

### Aesthetic Direction

- **ビジュアルの芯**: Radix Sage 系に寄せたニュートラルパレット（`src/styles/styles.css` の `@theme`）、Noto Sans JP Variable、テキスト階層と余白でリズムを作る。装飾は補助であり、内容が主役。
- **テーマ**: **prefers-color-scheme に追従**することを目標とする（現状はレイアウト上 `light` 固定とダーク用トークンが共存しているため、実装時は `html` のクラスまたは `color-scheme` とトークン切替を整合させる）。
- **明示的に避けたいもの（アンチリファレンス）**: 典型的な「AI スロップ」美学——ネオンアクセントの乱用、意味の薄いガラスモーフィズムの多用、汎用カードグリッドの量産、グラデーション見出しの安易な使用など。**個性はタイポ・余白・色の統制から出す。**

### Design Principles

1. **信頼は静けさから**: 動き・装飾・コントラストは必要最小限。ヒーローや実績は「読む順序」が一目でわかることを優先する。
2. **WCAG 2.2 AA を満たすことを設計の前提にする**: コントラスト、キーボード操作、フォーカス、レスポンシブ時の主要導線（特にナビ）を欠かさない。
3. **テーマはユーザーのシステム設定に誠実に**: ライト／ダークの両方でトークンが意味を持ち、切り替え時に破綻しないこと。
4. **トークンと意味の一貫性**: 色・余白・半径は CSS 変数／Tailwind テーマに寄せ、ハードコードの例外は理由とセットで残す。
5. **AI っぽさは機能ではない**: トレンドの装飾より、採用担当が求める情報（実績・役割・成果・連絡手段）の明瞭さを常に勝たせる。