name: PR Preview Deployment

on:
  pull_request:
    types: [opened, synchronize, reopened]

# PR毎に1つのプレビュー環境
concurrency:
  group: preview-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  NODE_ENV: production

jobs:
  # Note: Vercel handles preview deployments automatically via GitHub integration
  # This workflow is kept minimal - Vercel will build and deploy on each PR automatically
