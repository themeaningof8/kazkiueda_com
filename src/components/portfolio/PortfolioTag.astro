---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span
  class="inline-block max-w-full break-words rounded-full bg-surface-subtle px-3 py-1 text-xs tracking-widest text-fg-muted uppercase"
  >{label}</span
>
