---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span
  class="inline-block max-w-full break-words rounded-full border border-accent/25 bg-accent/10 px-3 py-1 text-xs font-medium tracking-widest text-accent uppercase"
  >{label}</span
>
