---
interface Props {
  email: string;
  headline?: string;
  subcopy?: string;
  buttonLabel?: string;
}

const {
  email,
  headline = '一緒に、意味のあるものをつくりましょう。',
  subcopy = 'プロジェクトのご相談や採用についてなど、お気軽にご連絡ください。',
  buttonLabel = 'お問い合わせ',
} = Astro.props;
---

<section
  id="contact"
  class="scroll-mt-28 border-y border-border-subtle/40 bg-accent-surface px-6 py-20 text-center text-fg-on-accent md:scroll-mt-32 md:px-12 md:py-28"
  aria-labelledby="contact-heading"
>
  <div class="mx-auto max-w-3xl">
    <h2
      id="contact-heading"
      class="text-3xl font-bold leading-tight tracking-tighter md:text-5xl"
    >
      {headline}
    </h2>
    <p class="mt-6 text-base leading-relaxed text-fg-on-accent md:text-lg">
      {subcopy}
    </p>
    <a
      class="mt-10 inline-block rounded-lg border border-border-subtle bg-surface-canvas px-10 py-4 text-base font-semibold text-fg-strong transition-colors hover:bg-surface-subtle motion-reduce:transition-none"
      href={`mailto:${email}`}
    >
      {buttonLabel}
    </a>
  </div>
</section>
